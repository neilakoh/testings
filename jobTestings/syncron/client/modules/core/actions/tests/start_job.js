const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../start_job';

describe('core.actions.start_job', () => {
  it('should do something');
});
