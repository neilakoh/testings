const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../test_item';

describe('core.actions.test_item', () => {
  it('should do something');
});
