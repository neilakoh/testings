const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../favorites_view';

describe('core.actions.favorites_view', () => {
  it('should do something');
});
