const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../product_lists';

describe('core.actions.product_lists', () => {
  it('should do something');
});
