const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../products';

describe('core.actions.products', () => {
  it('should do something');
});
