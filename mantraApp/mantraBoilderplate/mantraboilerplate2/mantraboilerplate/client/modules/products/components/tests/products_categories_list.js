const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import ProductsCategoriesList from '../products_categories_list';

describe('products.components.products_categories_list', () => {
  it('should do something');
});
